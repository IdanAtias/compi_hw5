// Init Ints 
{
	int a;
	int b = 1;
	int c = a;
	int d = b;
	int test = 1;
	
	if (a == 0 && b == 1 && c == 0 && d == 1)
		test = 0;
	if (a != 0 || b != 1 || c != 0 || d != 1 || test == 1) {
		print "FAIL";
	}
	else {
		print "OK";
	}
}
